---
description: Run a focused browser smoke for the Arvectum demo or specified journey
agent: build
---

Load the `browser-smoke` skill.

Journey or changed screen:

$ARGUMENTS

Use the documented local entrypoint and synthetic data. Verify visible checkpoints, failed requests, console errors, and downloads. Capture only useful failure evidence and do not replace focused API/unit tests.