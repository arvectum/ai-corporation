---
description: Reproduce and fix a concrete bug with a focused regression test
agent: build
---

Load the `bug-investigation` skill and investigate this issue:

$ARGUMENTS

Start with the narrowest reproducible path. Do not refactor unrelated code. Run the failing test or reproduction first, implement the minimal fix, add regression evidence, and summarize commands actually run.